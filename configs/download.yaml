# Datasets for tools/download_dataset.py, fetched via mirdata.
# Documented in docs/source/configuration.rst.
data_home: ../musicality_db
datasets:
  - ballroom
  - rwc_classical
  - rwc_jazz
  - rwc_popular
  - jtd
# gtzan is deliberately absent — mirdata's audio link is dead. See the docs for
# the HuggingFace mirror route.
