data_home: ../musicality_db
datasets:
  - ballroom
  - rwc_classical
  - rwc_jazz
  - rwc_popular
  # gtzan_genre: mirdata's loader has a dead audio link (opihi.cs.uvic.ca).
  # Use data_home/dl_gtzan.py instead (HuggingFace marsyas/gtzan mirror) to
  # populate data_home/gtzan/, drop in beat annotations under
  # gtzan/annotations/beats/, then tools/migrate_gtzan.py.
